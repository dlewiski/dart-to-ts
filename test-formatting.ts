// Test file with intentionally bad formatting
function testFunction() {
  console.log('This has bad formatting');
  const x = 5;
  return x;
}

// Extra whitespace
const y = 10;

export { testFunction, y };
